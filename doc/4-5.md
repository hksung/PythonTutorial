---
layout: page
title: Tuples, dictionaries, functions, classes, files 5. Working with files
---

[Back to Module 4](4.html)

# Working with files

In text processing, you often save data to a file or read it back later.

## Write a text file

```python
data = ["apple", "banana", "cherry"]

with open("fruits.txt", "w", encoding="utf-8") as f:
    for fruit in data:
        f.write(fruit + "\n")
```

## Read a text file

```python
with open("fruits.txt", "r", encoding="utf-8") as f:
    lines = f.readlines()
print(lines)
```

## CSV and JSON

```python
import csv
import json
```

Use `csv.writer`, `csv.DictReader`, `json.dump`, and `json.load` when you need structured data.

[Next: Exercises](4-6.html)